services:
  db:
    image: postgres:16
    container_name: mergesignal-db
    environment:
      POSTGRES_USER: mergesignal
      POSTGRES_PASSWORD: mergesignal
      POSTGRES_DB: mergesignal
    ports:
      - "5433:5432"
    volumes:
      - mergesignal_pg:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: mergesignal-redis
    ports:
      - "6379:6379"

volumes:
  mergesignal_pg:
