{
  "name": "mergesignal",
  "private": true,
  "description": "Security analysis platform for npm dependencies",
  "version": "0.1.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "precommit": "pnpm build && pnpm check-types && pnpm lint && pnpm test",
    "knip": "knip",
    "ci:verify": "rm -rf node_modules .turbo && pnpm install --frozen-lockfile && pnpm precommit",
    "prepare": "husky",
    "mergesignal": "pnpm -C apps/cli exec -- tsx src/cli.ts",
    "ms": "pnpm -C apps/cli exec -- tsx src/cli.ts"
  },
  "devDependencies": {
    "knip": "^5.88.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.4.3",
    "turbo": "^2.8.11",
    "typescript": "5.9.2"
  },
  "lint-staged": {
    "apps/web/**/*.{ts,tsx}": [
      "pnpm exec eslint -c apps/web/eslint.config.js --fix --max-warnings 0",
      "prettier --write"
    ],
    "apps/api/**/*.{ts,tsx}": [
      "pnpm exec eslint -c apps/api/eslint.config.js --fix --max-warnings 0",
      "prettier --write"
    ],
    "apps/cli/**/*.{ts,tsx}": [
      "pnpm exec eslint -c apps/cli/eslint.config.js --fix --max-warnings 0",
      "prettier --write"
    ],
    "apps/worker/**/*.{ts,tsx}": [
      "pnpm exec eslint -c apps/worker/eslint.config.js --fix --max-warnings 0",
      "prettier --write"
    ],
    "packages/**/*.{ts,tsx}": [
      "prettier --write"
    ],
    "*.{json,md,yaml,yml}": [
      "prettier --write"
    ]
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.19"
  },
  "volta": {
    "node": "22.15.1"
  }
}
