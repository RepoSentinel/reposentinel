DATABASE_URL="postgresql://mergesignal:mergesignal@localhost:5433/mergesignal?schema=public"
REDIS_URL="redis://localhost:6379"
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
# RATE_LIMIT_PUBLIC=60          # requests per minute for public endpoints (default: 60)
# RATE_LIMIT_AUTHENTICATED=1000 # requests per minute for authenticated endpoints (default: 1000)

# GitHub App (Step 9: GitHub Native Integration)
# - GITHUB_PRIVATE_KEY should be the PEM contents; if stored as single line, use literal \n
GITHUB_APP_ID=
GITHUB_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=

